name="musl-1.1.19"
srcdir="$SOURCES/$name"
tarball="$name.tar.gz"
fetchurl="http://www.musl-libc.org/releases/$tarball"

bld() {
	cd $srcdir
	CROSS_COMPILE=$XTARGET- \
	./configure \
		--prefix=/ \
		--target=$XTARGET
	make $MKOPTS
	make DESTDIR=$TOOLS/$XTARGET install
}
