pkgname=host-file
pkgver=5.35
pkgrel=1
arch=('any')
source=(ftp://ftp.astron.com/pub/file/file-$pkgver.tar.gz)

build() {
	cd "$srcdir"/file-$pkgver
	./configure \
		--prefix="$TOOLS"
	make
}

package() {
	cd "$srcdir"/file-$pkgver
	make install
}
