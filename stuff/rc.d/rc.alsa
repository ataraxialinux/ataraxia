#!/usr/bin/bash

case $1 in
	start)
		if [ ! -f "/var/lib/alsa/asound.state" ]; then
			mkdir -p /var/lib/alsa/
			alsactl restore
			alsactl store
		fi
		alsactl restore
		;;
	stop)
		alsactl store
		;;
	restart|reload)
		$0 stop
		$0 start
		;;
esac
