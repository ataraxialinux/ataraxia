#!/usr/bin/bash

NAME="watchdog"
OPTIONS="-t PERIOD /dev/watchdog"
PIDFILE="/run/$NAME.pid"

case $1 in
	start)
		mount -a -t nfs
		;;
	stop)
		umount -a -t nfs,nfs4
		;;
	restart|reload)
		$0 stop
		$0 start
		;;
esac
