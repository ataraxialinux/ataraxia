#!/bin/sh
#

exec 2>&1

TARGET="$1"
SVNAME="${2:-tty5}"

start() {
	echo "Starting ${SVNAME}..."
	exec getty 38400 tty5 linux
}

reset() {
	case $3 in
		exit)
			echo "Stopping ${SVNAME}..."
			;;
		signal)
			echo "Killing ${SVNAME}..."
			;;
		*)
			echo "Stopping ${SVNAME}..."
			;;
	esac
}

eval ${TARGET} "$@"
