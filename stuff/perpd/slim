#!/bin/sh
#

exec 2>&1

TARGET="$1"
SVNAME="${2:-slim}"

start() {
	echo "Starting ${SVNAME}..."
	exec /usr/bin/slim -nodaemon
}

reset() {
	case $3 in
		exit)
			echo "Stopping ${SVNAME}..."
			;;
		signal)
			echo "Killing ${SVNAME}..."
			;;
		*)
			echo "Stopping ${SVNAME}..."
			;;
	esac
}

eval ${TARGET} "$@"
