pkgname=chroot-fortify-headers
pkgver=1.0
pkgrel=1
arch=('any')
source=("http://dl.2f30.org/releases/fortify-headers-$pkgver.tar.gz")

package() {
	cd "$srcdir"/fortify-headers-$pkgver
	make PREFIX=/tools DESTDIR="$pkgdir" install
}
