# Description: Bootstrap configuration files
# URL:         https://januslinux.github.io/
# Maintainer:  protonesso, nagakamira at gmail dot com

name=chroot-etc
version=2018.08
release=1

build() {
	mkdir -p $PKG/etc

cat > $PKG/etc/profile << "EOF"
PATH="/usr/bin:/tools/bin"
export PATH
EOF
}
