name=chroot-file
version=5.34
release=1
source=(ftp://ftp.astron.com/pub/file/file-$version.tar.gz)

build() {
	cd file-$version
	./configure \
		--prefix=/tools \
		--build=$XHOST \
		--host=$XTARGET \
		--disable-nls
	make
	make DESTDIR=$PKG install
}
