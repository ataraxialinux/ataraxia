#!/bin/sh

if [ "$1" = "pre-install" ]; then
	getent group pulse-access || addgroup -S pulse-access 2>/dev/null
	getent group pulse || addgroup -S pulse 2>/dev/null
	getent passwd pulse || adduser -S -D -H -h /var/run/pulse -g "PulseAudio daemon" -G pulse pulse 2>/dev/null
fi
