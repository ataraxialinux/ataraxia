# Description: A suckless variant of the Linux system utilities
# URL:         https://core.suckless.org/ubase/
# Maintainer:  protonesso, nagakamira at gmail dot com

name=ubase
version=94b84f9c7f34692af5dd1b63eb7a95a0bdb9e3b6
release=1
options=('bootstrap')
source=("https://github.com/protonesso/ubase/archive/$version.tar.gz")

build() {
	cd "$SRC"/$name-$version
	if [ "$USEBOOTSTRAP" = "yes" ]; then
		make HOSTCC="$HOSTCC" CC="$CC" CFLAGS="$CFLAGS" LDFLAGS='-static' respawn halt ctrlaltdel killall5
	else
		make CFLAGS="$CFLAGS" LDFLAGS='-static' respawn halt ctrlaltdel killall5
	fi

	for cmd in respawn halt ctrlaltdel killall5; do
		install -D $cmd "$PKG"/usr/bin/$cmd
	done
}
