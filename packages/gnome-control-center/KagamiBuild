# Description: GNOME's main interface to configure various aspects of the desktop
# URL:         https://gitlab.gnome.org/GNOME/gnome-control-center
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  meson accountsservice clutter-gtk colord-gtk gnome-online-accounts gnome-settings-daemon opensettings grilo gsound libgtop libpwquality krb5 shared-mime-info cups-pk-helper samba gnome-bluetooth ibus network-manager-applet libgnomekbd libhandy sound-theme-freedesktop
# Section:     gnome

name=gnome-control-center
version=3.34.2
case $version in
	*.*.*.*) _v=${version%.*}
		_v=${_v%.*};;
	*.*.*) _v=${version%.*};;
esac
release=3
source=("http://ftp.gnome.org/pub/gnome/sources/$name/${_v}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	patch -Np1 -i "$STUFF"/gnome-control-center/0001-restore-consolekit-support.patch
	patch -Np1 -i "$STUFF"/gnome-control-center/0001-fix-musl-strlen.patch

	# get rid ofimedate1
	patch -Np1 -i "$STUFF"/gnome-control-center/0001-don-t-use-systemd-timdated.patch
	patch -Np1 -i "$STUFF"/gnome-control-centerb877da2862c8a3bdd9a071dcd55e6ac74f3b3570.patch

	sed -i '/ln -s/s/s /sf /' panels/user-accounts/meson.build

	mkdir -p build
	cd build
	ataraxia-meson "$SRC"/$name-$version \
		-Dcheese=false \
		-Ddocumentation=false \
		-Dgnome_session_libexecdir=/usr/lib
	ninja
	DESTDIR="$PKG" ninja install
}
