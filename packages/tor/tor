#!/bin/sh
TORHOME=/run/tor
if [ ! -d "$TORHOME" ] ; then
	mkdir -p "$(dirname $TORHOME)"
	addgroup -S tor && adduser -h "$TORHOME" -S -G tor tor
	su -s /bin/sh - tor -c "mkdir -p env ; echo $TORHOME > env/HOME"
fi
exec chpst -e $TORHOME/env -u tor tor 2>&1
