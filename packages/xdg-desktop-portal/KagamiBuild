# Description: Desktop integration portal
# URL:         https://github.com/flatpak/xdg-desktop-portal
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  python json-glib fuse2 pipewire geoclue
# Section:     libs

name=xdg-desktop-portal
version=1.6.0
release=4
source=("https://github.com/flatpak/xdg-desktop-portal/releases/download/$version/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	patch -Np1 -i "$STUFF"/xdg-desktop-portal/pipewire-0.3.patch
	autoreconf -vif

	./configure $BUILDFLAGS \
		--prefix=/usr \
		--libexecdir=/usr/lib \
		--disable-docbook-docs \
		--disable-libportal
	make
	make DESTDIR="$PKG" install
}
