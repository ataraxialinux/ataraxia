post_install() {
	grep -q '/usr/bin/zsh' etc/shells || add-shell /usr/bin/zsh
	grep -q '/bin/zsh' etc/shells || add-shell /bin/zsh
}

post_upgrade() {
	post_install
}

pre_remove() {
	remove-shell /usr/bin/zsh
	remove-shell /bin/zsh
}
