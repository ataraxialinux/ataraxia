# Description: A library that provides a GDI+-compatible API on non-Windows operating systems
# URL:         https://www.mono-project.com/docs/gui/libgdiplus/
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  zlib cairo freetype tiff libjpeg-turbo libpng giflib libexif
# Section:     libs

name=libgdiplus
version=6.0.5
release=1
source=("https://github.com/mono/libgdiplus/archive/$version/$name-$version.tar.gz")

build() {
	cd "$SRC"/$name-$version
	autoreconf -fiv
	./configure $BUILDFLAGS \
		--prefix=/usr
	make
	make DESTDIR="$PKG" install
}
