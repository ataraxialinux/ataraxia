post_install() {
	grep -q '/bin/mksh' etc/shells || add-shell /bin/mksh
	grep -q '/usr/bin/mksh' etc/shells || add-shell /usr/bin/mksh
}

post_upgrade() {
	post_install
}

pre_remove() {
	remove-shell /bin/mksh
	remove-shell /usr/bin/mksh
}
