# Description: A fast and easy-to-use status bar
# URL:         https://github.com/polybar/polybar
# Maintainer:  dotnix (aka XyzenX)
# Depends on:  cairo libxcb python xcb-proto xcb-util-image xcb-util-wm cmake pulseaudio wireless_tools make
# Section:     x11

name=polybar
version=3.4.3
release=0
source=("https://github.com/$name/$name/releases/download/$version/$name-$version.tar")

build() {
	cd "$SRC/$name"
	
	mkdir build
	cd build
	
	cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release ..
	cmake --build .
	
	cmake --build "$SRC/$name/build" --target install -- DESTDIR="$PKG"
}
