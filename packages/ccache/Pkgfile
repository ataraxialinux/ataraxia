# Description: Compiler cache that speeds up recompilation by caching previous compilations
# URL:         https://ccache.samba.org/
# Maintainer:  nee-san, nagakamira at gmail dot com
# Depends on:  libz

name=ccache
version=3.5
release=1
source=("https://www.samba.org/ftp/$name/$name-$version.tar.xz")
BOOTSTRAP=yes

build() {
	cd "$SRC"/$name-$version
	./configure $BUILDFLAGS \
		--prefix='' \
		--sysconfdir=/etc
	make

	install -Dm 755 ccache -t "$PKG"/bin

	install -d "$PKG"/lib/ccache/bin
	local _prog
	for _prog in gcc g++ c++; do
		ln -sf /bin/ccache "$PKG"/lib/ccache/bin/$_prog
		ln -sf /bin/ccache "$PKG"/lib/ccache/bin/${CHOST}-$_prog
	done
	for _prog in cc clang clang++; do
		ln -sf /bin/ccache "$PKG"/lib/ccache/bin/$_prog
	done
}
