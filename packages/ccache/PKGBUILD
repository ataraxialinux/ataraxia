# Maintainer: protonesso <nagakamira@gmail.com>

pkgname=ccache
pkgver=3.4.2
pkgrel=1
pkgdesc='Compiler cache that speeds up recompilation by caching previous compilations'
arch=('x86_64' 'aarch64' 'armhf' 'ppc64le' 's390x')
url='https://ccache.samba.org/'
license=('GPL')
groups=('base')
depends=('musl' 'zlib')
source=("https://www.samba.org/ftp/ccache/ccache-$pkgver.tar.xz")

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--build=$($HOSTCC -dumpmachine)
	make
}

package() {
	unset MAKEFLAGS
	cd "$srcdir/$pkgname-$pkgver"
	install -Dm 755 ccache $pkgdir/usr/bin/ccache

	local link=
	mkdir -p $pkgdir/usr/lib/ccache/bin

	for link in cc gcc g++ cpp c++ $XTAREGT-cc $XTAREGT-gcc \
			$XTAREGT-g++ $XTAREGT-cpp $XTAREGT-c++; do
		ln -sf /usr/bin/ccache $pkgdir/usr/lib/ccache/bin/$link
	done

	msg "Stripping package $pkgname..."
	find $pkgdir -type f | xargs file 2>/dev/null | grep "LSB executable"       | cut -f 1 -d : | xargs $STRIP --strip-all			2>/dev/null || true
	find $pkgdir -type f | xargs file 2>/dev/null | grep "shared object"        | cut -f 1 -d : | xargs $STRIP --strip-unneeded		2>/dev/null || true
	find $pkgdir -type f | xargs file 2>/dev/null | grep "current ar archive"   | cut -f 1 -d : | xargs $STRIP --strip-debug		2>/dev/null || true
	cd $pkgdir
	rm -rf usr/{,local/}{,share/}{doc,man,info,locale} usr{,/local}{,/share},opt/*}/{man,info} usr/{,local/}{,share/}{doc,gtk-doc} opt/*/{doc,gtk-doc}
	rm -rf $pkgdir/{,usr/}lib/charset.alias
}
md5sums=('9e048f88f3897125864f9a5e1abfb72d')
