# Description: Next generation desktop shell
# URL:         https://wiki.gnome.org/Projects/GnomeShell
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  meson evolution-data-server gjs gnome-control-center libcroco mutter startup-notification gnome-bluetooth network-manager-applet dconf gnome-menus gnome-themes-extra adwaita-icon-theme wallpapers
# Section:     gnome

name=gnome-shell
version=3.32.2
release=1
source=("http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	mkdir -p build
	cd build
	ataraxia-meson "$SRC"/$name-$version \
		-Dsystemd=false
	ninja
	DESTDIR="$PKG" ninja install
}
