# Description: Next generation desktop shell
# URL:         https://wiki.gnome.org/Projects/GnomeShell
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  meson evolution-data-server gjs libcroco mutter startup-notification gnome-bluetooth gnome-control-center network-manager-applet dconf gdm gnome-menus gnome-themes-extra adwaita-icon-theme wallpapers cantarell-fonts
# Section:     gnome

name=gnome-shell
version=3.32.2
release=2
source=("http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	mkdir -p build
	cd build
	ataraxia-meson "$SRC"/$name-$version \
		-Dman=false \
		-Dsystemd=false
	ninja
	DESTDIR="$PKG" ninja install
}
