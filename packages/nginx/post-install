#!/bin/sh
busybox adduser -s /usr/bin/nologin -D -h /usr/share/nginx -S nginx || true
install -o nginx -g nobody -d /var/log/nginx /var/tmp/nginx || true
ln -s ../sites-available/99-default /etc/nginx/sites-enabled/ 2>/dev/null || true
