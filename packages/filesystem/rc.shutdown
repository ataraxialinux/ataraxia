#!/bin/sh

# Append our default paths
appendpath () {
    case ":$PATH:" in
        *:"$1":*)
            ;;
        *)
            PATH="${PATH:+$PATH:}$1"
    esac
}

appendpath '/usr/bin'
appendpath '/usr/local/bin'
appendpath '/usr/local/sbin'
unset appendpath

export PATH

clear

hwclock -u -w

sv down /var/service/*

sync

killall5 -15
sleep 5
killall5 -9

swapoff -a

umount -a -r

mount -o remount,ro /
