#!/bin/sh

if [ "$1" = "pre-install" ]; then
	getent group colord || addgroup -S colord 2>/dev/null
	getent passwd colord || adduser -S -D -h /var/lib/colord -s /usr/bin/nologin -G colord -g colord colord 2>/dev/null
fi
