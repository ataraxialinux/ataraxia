#!/bin/sh

if [ "$1" = "pre-install" ]; then
	getent group unbound || addgroup -S unbound 2>/dev/null
	getent passwd unbound || adduser -S -D -H -h /etc/unbound -s /usr/bin/nologin -G unbound -g unbound unbound 2>/dev/null
fi
