# Description: A compat layer/bridge from libmodplug to libopenmpt
# URL:         http://lib.openmpt.org/
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  libopenmpt
# obsoletes:   libmodplug
# Section:     audio

name=libopenmpt-modplug
version=0.8.9.0+openmpt1
release=1
source=("https://lib.openmpt.org/files/$name/$name-${version/+/-}.tar.gz")

build() {
	cd "$SRC"/$name-${version/+/-}
	./configure $BUILDFLAGS \
		--prefix=/usr \
		--enable-libmodplug
	make
	make DESTDIR="$PKG" install
}
