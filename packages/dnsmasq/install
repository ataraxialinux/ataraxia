#!/bin/sh

if [ "$1" = "pre-install" ]; then
	getent group dnsmasq || addgroup -S dnsmasq 2>/dev/null
	getent passwd dnsmasq || adduser -S -D -H -h /var/empty -s /usr/bin/nologin -G dnsmasq -g dnsmasq dnsmasq 2>/dev/null
fi
