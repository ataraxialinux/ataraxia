# Description: run scripts or programs in a directory
# URL:         https://januslinux.github.io/
# Maintainer:  nee-san, nagakamira at gmail dot com

name=run-parts
version=1.4.8.8
release=1
BOOTSTRAP=yes

build() {
	mkdir -p "$PKG"/usr/bin

	${CROSS_COMPILE}cc $CFLAGS "$KEEP"/run-parts/run-parts.c -o "$PKG"/usr/bin/run-parts
}
