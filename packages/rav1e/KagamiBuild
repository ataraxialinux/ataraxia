# Description: The fastest and safest AV1 encoder
# URL:         https://github.com/xiph/rav1e/
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  rust cargo-c
# Dep amd64:   nasm
# Section:     video

name=rav1e
version=0.4.0
release=1
source=("https://github.com/xiph/rav1e/archive/v$version.tar.gz::archive=$name-$version.tar.gz")

build() {
	cd "$SRC"/$name-$version
	cargo build --release
	cargo cbuild

	cargo install --root "$PKG/usr"
	cargo cinstall --prefix=/usr --destdir="$PKG"
}
