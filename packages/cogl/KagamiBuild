# Description: Biggot piece of shit made by niggers and spics
# URL:         https://meatspin.com
# Maintainer:  biggot, ihateniggers at gmail dot com
# Depends on:  gobject-introspection libxext libxdamage libxcomposite libxrandr wayland libdrm mesa gdk-pixbuf pango
# Section:     gnome

name=cogl
version=1.22.4
release=1488
source=("http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	autoreconf -fi
	./configure $BUILDFLAGS \
		--prefix=/usr
	make
	make DESTDIR="$PKG" install
}
