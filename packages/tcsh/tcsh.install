post_install() {
	grep -q '/bin/csh' etc/shells || add-shell /bin/csh
	grep -q '/bin/tcsh' etc/shells || add-shell /bin/tcsh
}

post_upgrade() {
	post_install
}

pre_remove() {
	remove-shell /bin/csh
	remove-shell /bin/tcsh
}
