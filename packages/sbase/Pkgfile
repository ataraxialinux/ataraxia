# Description: A suckless variant of the *nix core utilities
# URL:         https://core.suckless.org/sbase/
# Maintainer:  nee-san, nagakamira at gmail dot com

name=sbase
version=c30255e50a77f20fb48bed7c700ff32f0d0dc489
release=1
source=("https://github.com/nee-san/sbase/archive/$version.tar.gz")
BOOTSTRAP=yes

build() {
	cd "$SRC"/$name-$version
	if [ "$CROSS" = "yes" ]; then
		make HOSTCC="$HOSTCC" CC="$CC" CFLAGS="$CFLAGS" LDFLAGS='-static'
		make HOSTCC="$HOSTCC" CC="$CC" CFLAGS="$CFLAGS" LDFLAGS='-static' PREFIX='' MANPREFIX='/usr/share/man' DESTDIR="$PKG" install
	else
		make CFLAGS="$CFLAGS" LDFLAGS='-static'
		make CFLAGS="$CFLAGS" LDFLAGS='-static' PREFIX='' MANPREFIX='/usr/share/man' DESTDIR="$PKG" install
	fi

	rm -rf "$PKG"/usr/bin/{ed,getconf,grep,hostname,sed,strings,tftp}
}
