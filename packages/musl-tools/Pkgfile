# Description: Misc utilities for musl libc
# URL:         https://januslinux.github.io/
# Maintainer:  nee-san, nagakamira at gmail dot com

name=musl-tools
version=20190213
release=1
BOOTSTRAP=yes

build() {
	mkdir -p "$PKG"/bin

	for file in getconf getent iconv; do
		${CROSS_COMPILE}cc $CFLAGS "$KEEP"/musl-tools/${file}.c -o "$PKG"/bin/$file
	done
}
