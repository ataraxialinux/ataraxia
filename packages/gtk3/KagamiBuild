# Description: GObject-based multi-platform GUI toolkit
# URL:         https://www.gtk.org/
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  meson at-spi2-atk fribidi gdk-pixbuf librsvg libepoxy pango hicolor-icon-theme iso-codes libxcomposite libxcursor libxdamage libxi libxinerama libxkbcommon libxrandr sassc wayland-protocols cups json-glib rest
# Section:     libs

name=gtk3
version=3.24.20
release=2
source=("http://ftp.gnome.org/pub/gnome/sources/gtk+/${version%.*}/gtk+-$version.tar.xz")

build() {
	cd "$SRC"/gtk+-$version
	patch -Np1 -i "$STUFF"/gtk3/0001-gtk-don-t-use-check-version.py.patch

	mkdir -p build
	cd build

	ataraxia-meson "$SRC"/gtk+-$version \
		-Dbroadway_backend=true \
		-Dcolord=yes \
		-Dgtk_doc=false \
		-Dman=false \
		-Dprint-backends=true
	ninja
	DESTDIR="$PKG" ninja install
}
