# Description: A VNC Client for the GNOME desktop
# URL:         https://wiki.gnome.org/Apps/Vinagre
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  intltool itstool vala appstream-glib libsecret gtk-vnc freerdp telepathy-glib vte dconf
# Section:     gnome

name=vinagre
version=3.22.0
release=1
source=("http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	patch -Np1 -i "$STUFF"/vinarge/freerdp.patch
	autoreconf -vif

	./configure $BUILDFLAGS \
		--prefix=/usr \
		--libexecdir=/usr/lib/vinarge \
		--sysconfdir=/etc \
		--enable-compile-warnings=minimum \
		--enable-rdp
	make
	make DESTDIR="$PKG" install
}
