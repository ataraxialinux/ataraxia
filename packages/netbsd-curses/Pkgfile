# Description: libcurses and dependencies taken from netbsd and brought into a portable shape
# URL:         https://github.com/sabotage-linux/netbsd-curses/
# Maintainer:  protonesso, nagakamira at gmail dot com

name=netbsd-curses
version=0.3.1
release=1
source=("http://ftp.barfooze.de/pub/sabotage/tarballs/$name-$version.tar.xz")
BOOTSTRAP=yes

build() {
	cd "$SRC"/$name-$version
cat << EOF > config.mak
CC=${CROSS_COMPILE}cc
HOSTCC=cc
AR=${CROSS_COMPILE}ar
RANLIB=${CROSS_COMPILE}ranlib
CFLAGS=-fPIC $CFLAGS
PREFIX=/usr
DESTDIR=$PKG
EOF
	make
	make install

	rm -rf "$PKG"/usr/share/man/man3/attr_get.3.gz "$PKG"/usr/share/man/man3/attr_set.3.gz
}
