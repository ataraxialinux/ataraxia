#!/bin/sh

if [ "$1" = "pre-install" ]; then
	getent group gdm || addgroup -S gdm 2>/dev/null
	getent passwd gdm || adduser -S -D -H -h /var/lib/gdm -s /usr/bin/nologin -G gdm -g gdm gdm 2>/dev/null
fi
