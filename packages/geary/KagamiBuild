# Description: A lightweight email client for the GNOME desktop
# URL:         https://wiki.gnome.org/Apps/Geary
# Maintainer:  protonesso, nagakamira at gmail dot com
# Depends on:  meson samurai gobject-introspection vala yelp-tools appstream-glib enchant folks gcr gmime gnome-keyring gnome-online-accounts gsound gspell libcanberra libgee libhandy libnotify libpeas libsecret libsoup libxml2 webkitgtk
# Section:     web

name=geary
version=3.38.1
release=1
source=("https://download.gnome.org/sources/$name/${version%.*}/$name-$version.tar.xz")

build() {
	cd "$SRC"/$name-$version
	mkdir -p build
	cd build
	ataraxia-meson "$SRC"/$name-$version
	samu
	DESTDIR="$PKG" samu install
}
