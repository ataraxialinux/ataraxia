name="make-4.2.1"
srcdir="$SOURCES/$name"
tarball="$name.tar.bz2"
fetchurl="http://ftp.gnu.org/gnu/m4/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET \
		--disable-nls
	make $MKOPTS
	make DESTDIR=$XDESTDIR install
}
