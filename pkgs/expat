name="expat-2.2.5"
srcdir="$SOURCES/$name"
tarball="$name.tar.bz2"
fetchurl="http://prdownloads.sourceforge.net/expat/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make $MKOPTS
	make DESTDIR=$XDESTDIR install
}
