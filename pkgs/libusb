name="libusb-1.0.22"
srcdir="$SOURCES/$name"
tarball="$name.tar.bz2"
fetchurl="https://github.com/libusb/libusb/releases/download/v1.0.22/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make $MKOPTS
	make DESTDIR=$XDESTDIR install
}
