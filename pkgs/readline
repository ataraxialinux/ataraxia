name="readline-7.0"
srcdir="$SOURCES/$name"
tarball="$name.tar.gz"
fetchurl="http://ftpmirror.gnu.org/gnu/readline/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make SHLIB_LIBS="-L$ROOTFS/usr/lib -lncursesw" $MKOPTS
	make DESTDIR=$ROOTFS install
}
