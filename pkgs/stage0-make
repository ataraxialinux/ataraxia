. $KEEP/common.sh

PKG=stage0-make
VER=4.2.1

tarxf http://ftp.gnu.org/gnu/make/ make-$VER .tar.bz2

pkgprep

./configure CROSS_COMPILE=$TARGET- \
	--build=$HOST \
	--host=$TARGET \
	--prefix=/usr \
	--without-guile \
	--disable-nls
make -j$JOBS
make DESTDIR=$OUT install

packpkg
installhostpkg
