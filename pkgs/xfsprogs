name="xfsprogs-4.15.1"
srcdir="$SOURCES/$name"
tarball="$name.tar.xz"
fetchurl="https://mirrors.edge.kernel.org/pub/linux/utils/fs/xfs/xfsprogs/$tarball"

bld() {
	cd $srcdir
	export DEBUG=-DNDEBUG
	export OPTIMIZER="$CFLAGS"
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET \
		--cache-file=$KEEP/m4.cache
	make SHELL=/bin/bash $MKOPTS
	make DESTDIR=$XDESTDIR install
	unset DEBUG OPTIMIZER
}
