name="dhcpcd-7.0.2"
srcdir="$SOURCES/$name"
tarball="$name.tar.xz"
fetchurl="https://roy.marples.name/downloads/dhcpcd/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make $MKOPTS
	make DESTDIR=$XDESTDIR install
}
