name="skalibs-2.6.4.0"
srcdir="$SOURCES/$name"
tarball="$name.tar.gz"
fetchurl="http://www.skarnet.org/software/skalibs/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make $MKOPTS
	make DESTDIR=$XDESTDIR install
}
