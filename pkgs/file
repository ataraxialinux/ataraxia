name="file-5.32"
srcdir="$SOURCES/$name"
tarball="$name.tar.gz"
fetchurl="ftp://ftp.astron.com/pub/file/$tarball"

bld() {
	cd $srcdir
	./configure \
		--prefix=/usr \
		--build=$XHOST \
		--host=$XTARGET
	make $MKOPTS
	make DESTDIR=$ROOTFS install
}
